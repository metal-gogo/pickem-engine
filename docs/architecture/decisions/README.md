# Architecture Decisions

Use this folder for architecture-specific decision records once technical choices become concrete enough to justify them.

Examples:

- result ingestion approach
- admin fallback strategy
- localization strategy
- scoring update execution model

Do not create architecture decisions just to speculate. Record them when a real choice has been made.
